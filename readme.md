# Simple cli app with Node.JS

Inside project folder
```bash
$ npm install -g cocktail-cli
```

Commands
```bash
$ cocktail random # for random cocktail
$ cocktail name "margarita" # search cocktail with name 
```